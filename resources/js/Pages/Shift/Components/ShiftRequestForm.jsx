import {Grid, TextField} from "@mui/material";

const ShiftRequestForm = ({onChange}) => {
    return <Grid container>
        <Grid item>
            <TextField />
        </Grid>
    </Grid>
}

export default ShiftRequestForm;
